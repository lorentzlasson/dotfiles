Stop hook enforcing a hard character budget on responses — block any response over N chars unless the user's message contained the literal word "elaborate", forcing a terser re-answer.

Mechanical enforcement of the concision rule, since prose instructions (CLAUDE.md, "tldr" anchor) have repeatedly failed to hold.
