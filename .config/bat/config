# Use terminal colors (inherits from alacritty dunkel theme)
--theme="ansi"

# Show line numbers, git changes, and header
--style="numbers,changes,header"

# Use less as pager with better options
--pager="less -FR"